# ==========================================
# 1. USER SERVICE
# ==========================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  labels:
    app: user-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
      - name: user-service
        image: thanhieub2305573/user-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
        env:
        - name: PORT
          value: "8000"
        - name: CLIENT_ID
          value: "373059704505-idbcgjtvf3pl94krkoeh67f4ars8fmte.apps.googleusercontent.com"
        - name: CLIENT_SECRET_KEY
          value: "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"
        - name: USERS_DB_URL  
          value: "postgresql://postgres:abc123@postgres:5432/user_db"
        - name: FRONTEND_HOST
          value: "http://localhost:30080"
        - name: STORAGE_API_URL
          value: "http://storage-service:8000"
        - name: BACKEND_USER_URL
          value: "http://user-service:8000"
        - name: BACKEND_COURSE_URL
          value: "http://course-service:8001"
        - name: BACKEND_QUIZ_EXAM_URL
          value: "http://quiz-exam-service:8002"
        - name: BACKEND_LEARNING_PROGRESS_URL
          value: "http://learning-progress-service:8003"
        - name: COURSES_DB_URL
          value: "postgresql://postgres:abc123@postgres:5432/course_db"
        envFrom:
        - secretRef:
            name: backend-secrets
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
  selector:
    app: user-service

---
# ==========================================
# 2. COURSE SERVICE
# ==========================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: course-service
  labels:
    app: course-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: course-service
  template:
    metadata:
      labels:
        app: course-service
    spec:
      containers:
      - name: course-service
        image: thanhieub2305573/course-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8001
        env:
        - name: PORT
          value: "8001"
        - name: COURSES_DB_URL
          value: "postgresql://postgres:abc123@postgres:5432/course_db"
        - name: FRONTEND_HOST
          value: "http://localhost:30080"
        - name: STORAGE_API_URL
          value: "http://storage-service:8000"
        - name: BACKEND_USER_URL
          value: "http://user-service:8000"
        - name: BACKEND_COURSE_URL
          value: "http://course-service:8001"
        - name: BACKEND_QUIZ_EXAM_URL
          value: "http://quiz-exam-service:8002"
        - name: BACKEND_LEARNING_PROGRESS_URL
          value: "http://learning-progress-service:8003"
        - name: COURSES_DB_URL
          value: "postgresql://postgres:abc123@postgres:5432/course_db"
        envFrom:
        - secretRef:
            name: backend-secrets
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: course-service
spec:
  type: ClusterIP
  ports:
  - port: 8001
    targetPort: 8001
  selector:
    app: course-service

---
# ==========================================
# 3. QUIZ EXAM SERVICE
# ==========================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: quiz-exam-service
  labels:
    app: quiz-exam-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: quiz-exam-service
  template:
    metadata:
      labels:
        app: quiz-exam-service
    spec:
      containers:
      - name: quiz-exam-service
        image: thanhieub2305573/quiz-exam-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8002
        env:
        - name: PORT
          value: "8002"
        - name: QUIZS_DB_URL  
          value: "postgresql://postgres:abc123@postgres:5432/quiz_exam_db"
        - name: FRONTEND_HOST
          value: "http://localhost:30080"
        - name: STORAGE_API_URL
          value: "http://storage-service:8000"
        - name: BACKEND_USER_URL
          value: "http://user-service:8000"
        - name: BACKEND_COURSE_URL
          value: "http://course-service:8001"
        - name: BACKEND_QUIZ_EXAM_URL
          value: "http://quiz-exam-service:8002"
        - name: BACKEND_LEARNING_PROGRESS_URL
          value: "http://learning-progress-service:8003"
        - name: COURSES_DB_URL
          value: "postgresql://postgres:abc123@postgres:5432/course_db"
        envFrom:
        - secretRef:
            name: backend-secrets
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: quiz-exam-service
spec:
  type: ClusterIP
  ports:
  - port: 8002
    targetPort: 8002
  selector:
    app: quiz-exam-service

---
# ==========================================
# 4. LEARNING PROGRESS SERVICE
# ==========================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: learning-progress-service
  labels:
    app: learning-progress-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: learning-progress-service
  template:
    metadata:
      labels:
        app: learning-progress-service
    spec:
      containers:
      - name: learning-progress-service
        image: thanhieub2305573/learning-progress-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8003
        env:
        - name: PORT
          value: "8003"
        - name: LEARNING_PROGRESS_DB_URL
          value: "postgresql://postgres:abc123@postgres:5432/learning_progress_db"
        - name: FRONTEND_HOST
          value: "http://localhost:30080"
        - name: STORAGE_API_URL
          value: "http://storage-service:8000"
        - name: BACKEND_USER_URL
          value: "http://user-service:8000"
        - name: BACKEND_COURSE_URL
          value: "http://course-service:8001"
        - name: BACKEND_QUIZ_EXAM_URL
          value: "http://quiz-exam-service:8002"
        - name: BACKEND_LEARNING_PROGRESS_URL
          value: "http://learning-progress-service:8003"
        - name: COURSES_DB_URL
          value: "postgresql://postgres:abc123@postgres:5432/course_db"
        envFrom:
        - secretRef:
            name: backend-secrets
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: learning-progress-service
spec:
  type: ClusterIP
  ports:
  - port: 8003
    targetPort: 8003
  selector:
    app: learning-progress-service